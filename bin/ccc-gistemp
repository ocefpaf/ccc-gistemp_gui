#!/usr/bin/python
# $URL$
# $Rev$
#
# ccc-gistemp
#
# Filipe Fernandes, 2011-06-27

import sys
from CCCgistemp.tool import run

#TODO, add options to run vischeck

if __name__ == '__main__':
    if len(sys.argv) == 1:
        sys.exit(run.main())
    elif sys.argv[1] == 'gui':
        #TODO: Should renamed or add to CCCgistemp namespace.
        from gui import run_gui
        if 0:
            run_gui()  #FIXME: must fix splash screen first.
    else:
        sys.exit(run.main(argv=sys.argv))
